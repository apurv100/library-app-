this is a basic windows application which can be used to keep the record of books in a library or personal collection. To run the app download the files and run the frontend.py file
there is also an exe file of the program in dist file. the app uses sqlite as its database and tkinter library python for frontend while vanilla python for backend.
